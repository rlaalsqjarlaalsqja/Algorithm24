def find_min_max(A) :
    min = A[0]
    max = A[0]
    for i in range(1, len(A)) :
        if min > A[i]:
            min = A[i]
        if max < A[i]:
            max = A[i]
    return (min, max)

A = [5, 6, 8, 9, 1, 4]
B = find_min_max(A)
print(B)
    